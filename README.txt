Type "node server" in CLI to start the server. 

Then use Postman GET at http://localhost:3000/getNote and get the test file, 
and then POST at http://localhost:3000/updateNote/:note. The updated file will be shown in response.